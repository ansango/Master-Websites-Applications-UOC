import { RegisterOptions } from "react-hook-form";

type TextAreaProps = {
  label?: string;
  name: string;
  rows?: number;
  options?: RegisterOptions;
  register?: any;
};

export default TextAreaProps;
