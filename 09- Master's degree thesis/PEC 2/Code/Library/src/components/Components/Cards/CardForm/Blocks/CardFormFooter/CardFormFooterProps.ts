import { ReactElement } from "react";

type CardFormFooterProps = {
  /**
   * label: string;
   */
  label: string;
  /**
   * link?: ReactElement;
   */
  link?: ReactElement;
};

export default CardFormFooterProps