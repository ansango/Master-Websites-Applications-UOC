/* 

El reto de esta semana es el siguiente:

¿ Podéis escribir una única expresión en JavaScript para calcular el máximo de 3 números a, b y c que sólo use el operador condicional ternario y operadores de comparación (< o >) ?

Después intentad reescribirla usando sólo el operador condicional ternario y el operador Math.abs.

*/