/*

Escribid un programa en JavaScript que a partir del día de la semana en format numérico (1=lunes,2=martes,...,7=domingo), muestre por consola el nombre del día.

Ejemplos:

Si día es 1, entonces muestra por consola “lunes”
Si día es 4, entonces muestra por consola “jueves”

Escribid un programa en JavaScript que a partir de un valor numérico entero, muestre por consola “positivo” si es mayor que cero, “cero” si es 0, o “negativo” si es menor que cero. 

Escribid un programa en JavaScript que calcule el índice de masa corporal (IMC) de una persona de masa m (en kg) y altura a (en m), y muestre por pantalla este índice, y un texto que lo interprete según la seguiente tabla:

Si IMC es menor que 18, entonces muestra por consola “bajo”.
Si IMC es entre 18 y 25, entonces muestra por consola “normal”.
Si IMC es entre 25 y 30, entonces muestra por consola “sobrepeso”.
Si IMC es entre 30 y 40, entonces  muestra por consola “obesidad”.
Si IMC es mayor que 40, entonces muestra por consola “obesidad mórbida”.

El comando prompt abre una ventana en el navegador y pide al usuario que entre un valor. Estudiad el siguiente código y decid en qué caso podría dar error, de qué tipo, y cómo se podría evitar con try...catch...finally:

var n = prompt("Entra tu nombre");
console.log(n.length);

Estudiad el siguiente código y decid en qué caso podría dar error, de qué tipo, y cómo se podría evitar con try...catch...finally:

var n = prompt("Entra un número");
console.log(1/n);

Escribid un programa en JavaScript que, usando el comando switch, muestre por consola el máximo de tres números.

Escribid un programa en JavaScript que, usando el comando switch, mouestre per consola si un número es positivo, cero, o negativo.

Escribid un programa en JavaScript que calcule las soluciones de una ecuación de segundo grado de la forma a*x*x + b*x + c = 0, dados a, b y c. Usad try...catch...finally para gestionar posibles errores.

Escribid un programa en JavaScript que calcule el número de días de un mes concreto de un año concreto (tened en cuenta los años bisiestos), usando el comando switch. 

Escribid un programa en JavaScript que calcule el día de la semana de una fecha cualquiera, usando el comando switch.

*/